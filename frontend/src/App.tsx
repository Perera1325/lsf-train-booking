export default function App() {
  return (
    <div style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>LSF Train Booking</h1>
      <p>Colombo Fort → Badulla — segment-based seat booking.</p>
      <p style={{ color: "#888" }}>
        Booking UI arrives in Stage 4. Backend health check:{" "}
        <code>/api/health</code>
      </p>
    </div>
  );
}
